dns-speed
=========

Description
-----------
This script test the resolution time from several public DNS, so you can choose your fastest DNS.

Requirements
------------
Just make sure you have the dig command is part of bind-utils on Red-Hat like distros and dnsutils in Debian like distros.

Usage
-----

# Just run the script
./dns-speed.sh
